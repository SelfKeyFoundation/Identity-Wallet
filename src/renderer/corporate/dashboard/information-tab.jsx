import React from 'react';
import { withStyles } from '@material-ui/core';

const styles = theme => ({});

const CorporateInformationTab = withStyles(styles)(({ classes }) => <div />);

export { CorporateInformationTab };
export default CorporateInformationTab;
