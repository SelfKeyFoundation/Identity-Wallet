import React from 'react';
import { withStyles } from '@material-ui/core';

const styles = theme => ({});

const CorporateOverviewTab = withStyles(styles)(({ classes }) => <div />);

export { CorporateOverviewTab };
export default CorporateOverviewTab;
