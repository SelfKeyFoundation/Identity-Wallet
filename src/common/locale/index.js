/* istanbul ignore file */
import reducer from './reducers';

import * as localeOperations from './operations';
import * as localeTypes from './types';

export { localeOperations, localeTypes };

export default reducer;
