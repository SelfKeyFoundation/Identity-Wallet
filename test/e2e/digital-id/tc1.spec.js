it('TC01: Logs in to the wallet', function() {
	// Open ID Wallet Application
	// Click "Restore Wallet" button'
	// click('#restoreWallet')
	// Select "Keystore / Select"
	// click('#keystoreSelect')
	// Enter password
	// setValue('#password', password)
	// Click Unlock button
	// click('#unloock')
	// User is redirected to Selfkey Dashboard
	// waitForExists('#dashboardTitle')
	// assert title === dashboard
});
