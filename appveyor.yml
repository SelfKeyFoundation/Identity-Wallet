environment:
  nodejs_version: "8"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
  - npm run makewin

test_script:
  - node --version
  - npm --version
  - npm run test

build: off