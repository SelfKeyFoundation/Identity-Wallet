environment:
  nodejs_version: "9"
environment:
  OSENV: win
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
  - npm run makewin

test_script:
  - node --version
  - npm --version
  - npm run testwin

build: off