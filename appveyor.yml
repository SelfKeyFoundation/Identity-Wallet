environment:
  nodejs_version: "9"
  NODE_ENV: test
  OSENV: win
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
  - npm run makewin
test_script:
  - node --version
  - npm --version
  - npm run test
build: off
