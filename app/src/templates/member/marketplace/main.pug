.marketplace-main
    .center-container
        .header-wrapper
            .title Exchanges
            .status(layout='row')
                span Your Staking Status
                md-icon(md-svg-src="assets/images/icons/ic_help_outline_black_24px.svg" class="color-white")
                    md-tooltip.sk-tooltip(md-direction="bottom")
                        |  <div style="text-align:left;"> What is Staking? <br />
                        |  Staking acts similarly like <br />
                        |  a refundable security deposit. <br />
                        |  to learn more,click here.. </div>
                //sk-icon(icon="ic_help_outline_black_24px", size-class="icon-size-16x16")
                span : Coming soon
        .body-wrapper
            ul
                li(ng-repeat='item in marketplaceData')
                    .list-title(layout='row')
                        .title-logo
                            sk-icon(ng-if='item.logo', icon="{{item.logo}}", size-class="icon-size-36x36")
                        .title-text   {{item.title}}
                    .list-body
                        .body-description {{ item.description | limitTo:300 }}
                            span(ng-if='item.description.length > 300') ...
                        .buttons-section
                            md-button.gray(ui-sref="member.marketplace.exchange({data: item})",ng-disabled='!item.available')
                                span {{item.available ? 'View' : 'Coming Soon'}}
