.header
    span.none-select-text Create New Wallet
form.content(layout="column", style="padding: 24px;")
    
    span.sk-text type the password entered previous step
    span.sk-text make sure you remember the password

    md-input-container.sk-input-box.no-error-spacer
        input(name="password", ng-model="userInput.password", type="password", placeholder="Password")
    
    .sk-button.filled-blue(style="margin: 24px 0 12px 0;", ng-click="createKeystore($event)")
        span(style="color: #fff") Generate Wallet

    .sk-button.outline-blue(style="margin: 12px 0 48px 0;", ui-sref="guest.create.step-1")
        span(style="color: #fff") back