md-dialog.ledger-is-connecting-container(aira-lable="Connecting to Ledger")
    .main-container
        .header-conainer
            sk-icon.close-btn(icon="close-dialog", size-class="icon-size-36x36", ng-click="cancelConectToLedger($event)")
        .body-container
            .header-title {{isConnecting ? 'Connecting To Ledger...' : "Error: Can't Connect To Ledger"}}
            .items(ng-if="isConnecting") 
                .item.title To ensure a successful connection, please check if:
                .item 1) "Browser Support" is disabled in Ledger settings.
                .item 2) Your Ledger is properly plugged in.
                .item 3) You have entered the PIN and logged in.
                .item 4) The Ethereum application is open.
            .items(ng-if="!isConnecting")
                .item 1) Is "Browser Support" disabled in Ledger settings?
                .item 2) Is it properly plugged in?
                .item 3) Did you enter the PIN to unlock?
                .item 4) Is the Ethereum application open?
            .buttons-container.small-padding-top(ng-if="isConnecting")
                md-button.gray.md-ink-ripple.cancal-btn(ng-click="cancelConectToLedger($event)")
                    span.primary close
            .buttons-container(ng-if="!isConnecting")
                md-button.gray.md-ink-ripple.cancal-btn(ng-click="cancelConectToLedger($event)")
                    span.primary BACK
                md-button.gray.md-ink-ripple.cancal-btn(ng-click="connectToLedger($event)")
                    span.primary TRY AGAIN
