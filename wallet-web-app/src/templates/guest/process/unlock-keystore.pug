div(layout-fill, layout="column", layout-align="center center")
    img(src="assets/images/icons/important.svg", alt="")
    
    #message-container(layout="column", layout-align="center center" style="width: 80%; height: 48px;")

    .sk-box.shadowed(style="width: 80%;")
        .header 
            span(style="user-select: none;") Unlock Wallet
        .content(layout="column", style="padding: 24px;")
            div(layout="row")
                span.sk-text(style="user-select: none;") if the following public key not belongs to you try
                a.sk-hyperlink.sk-text(style="user-select: none;", ui-sref="guest.process.import-keystore") &nbsp;import&nbsp;
                span.sk-text(style="user-select: none;") your one.

            md-input-container
                md-select(ng-model="selectedPublicKey", ng-change="changeWallet($event, selectedPublicKey)")
                    md-option(value="{{key}}", ng-repeat="key in publicKeys") {{key}}

            .sk-input-container(style="margin: 12px 0 12px 0;")
                input.sk-input(type="password", name="password", placeholder="Use your password to decrypt private key", ng-model="keystorePassword")

            .sk-button.filled-blue(style="margin: 12px 0 12px 0;", ng-click="unlock($event)")
                span(style="color: #fff") Unlock
            .sk-button.outline-blue(style="margin: 12px 0 48px 0;", ui-sref="guest.welcome")
                span(style="color: #fff") back
            
            md-menu.sk-dropdown-menu(style="width:120px;")
                md-button(ng-click="openMenu($mdMenu, $event)" layout="row", layout-align="space-between center")
                    .title Actions
                    i.arrow
                md-menu-content.sk-dropdown-menu-content(width="4")
                    md-menu-item.sk-dropdown-menu-item()
                        md-button() item 0
                    md-menu-item.sk-dropdown-menu-item()
                        md-button() item 1

                